#include <Arduino.h>
#include <esp.h>

#define GPIO 2
#define CHAN 0
#define RESOLUTION 16

void setup() {
    Serial.begin(115200);
    pinMode(GPIO, OUTPUT);
    espDACSetup(GPIO, CHAN, RESOLUTION);
}
void loop() {
    espAnalogWrite_DimLED(CHAN);
}
